const nextConfig = {
  // Optimize for Tailwind CSS v4
  experimental: {
    optimizeCss: true,
  },
  // Optional: Configure turbopack root if needed
  // turbopack: {
  //   root: process.cwd()
  // }
};

export default nextConfig;
